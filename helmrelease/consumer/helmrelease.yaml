apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: consumer-release
  namespace: flux-system
spec:
  interval: 5m
  chart:
    spec:
      chart: ./charts/consumer
      sourceRef:
        kind: GitRepository
        name: github-source
        namespace: flux-system
      interval: 1m
  values:
    replicaCount: 1